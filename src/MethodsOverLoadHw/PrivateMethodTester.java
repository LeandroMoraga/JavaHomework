package MethodsOverLoadHw;

public class PrivateMethodTester {
        /*
        task:
        Create 1 class with a private method that has 3 overloaded forms.
        Then call each overloaded method with specific arguments and observe result.
         */
   /*
   "PrivateMethod." doesn't pop up anything because the method is private
   'private' Access Modifier which means the method is only available to the
    class it was created only
    */
}
